export enum ShadowName {
  State = 'state',
  Package = '$package',
  Schedule = 'schedule',
  Classic = ''
}

export enum SchedulingType {
  LOCAL = 'local',
  REMOTE = 'remote'
}

export enum SchedulingMode {
  DAILY = 'daily',
  WEEKLY = 'weekly'
}
